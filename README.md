# Rails Application - A Blog
### My first ever application created using Ruby on Rails

On a local machine, install the Gemfiles and start the server.
```sh
$ cd blog/
$ bundle install
$ rails server
```

